export interface TableTheme {
    defaults: {
        thBgColor: string;
        thTextColor: string;
        hoverTrBgColor: string;
        borderColor: string;
        nthTrBgColor: string;
    };
}
