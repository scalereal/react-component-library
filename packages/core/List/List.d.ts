import * as React from 'react';
import { WithStyle } from '../../utils';
import { ListProps } from './types';
export declare const variant: ListProps['variant'][];
export declare const List: React.FC<ListProps> & WithStyle;
export default List;
