export interface CheckBoxTheme {
    defaults:{
        textColor:string;
        errorTextColor:string;
        hoverBgColor:string;
        borderColor:string;
        shadowColor:string;
        bgColor:string;
    }
}