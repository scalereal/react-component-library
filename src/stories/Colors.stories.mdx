import { Canvas, Meta, Props, Story } from '@storybook/addon-docs/blocks';
import * as stories from './Colors.stories.tsx';
import { SingleShadeColors, MultiShadeColors } from './Colors.stories.tsx';
import Colors from './assets/colors.svg';


<style>{`
  .heading {
    font-weight: 900;
    font-size: 24px;
    color: #111;
    letter-spacing: 6px;
    line-height: 24px;
    text-transform: uppercase;
    margin-bottom: 12px;
    margin-top: 40px;
  }
   .subheading {
    font-weight: 700;
    font-size: 16px;
    color: #111;
    letter-spacing: 2px;
    line-height: 24px;
    margin-bottom: 12px;
    margin-top: 40px;
  }

  .head {
    display: flex;
    justify-content:flex-start;
  }
  
`}</style>


<Meta  title="Colors" />

<div className="head">
  <img src={Colors} alt="colors" />
  <div className="heading">Colors</div>
</div>

These colors can be used to create a color theme that reflects your brand or style.

<div className="subheading">How to Use</div>

```tsx
import { defaultTheme } from '@scaleReal-components/theme';

const color = defaultTheme.colorsTheme.blue[500];
```

<div  className="subheading">Single Shade Colors</div>

<Canvas withToolbar>
    <SingleShadeColors />
</Canvas>

<div className="subheading">Multi Shade Colors</div>

<Canvas withToolbar>
    <MultiShadeColors />
</Canvas>
