import CircleLoader2 from '.';
import { Preview, Story, Meta, Props, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';
import { withKnobs, color, boolean, select } from '@storybook/addon-knobs';
import { SizeMap } from '../types';

<Meta
    title="Components/Loaders/CircleLoader2"
    component={CircleLoader2}
    argTypes={{
        size: {
            control: {
                type: 'select',
                options: Object.values(SizeMap)
            }
        },
        Color: {
            control: 'color'
        },
        display: {
            control: {
                type: 'boolean'
            }
        },
        ref: {
            table: {
                disable: true
            }
        },
        theme: {
            table: {
                disable: true
            }
        },
        as: {
            table: {
                disable: true
            }
        }
    }}
/>

# Loaders

Displays a Loader.

export const Template = args => (
    <div>
        <CircleLoader2 {...args} />
    </div>
);

<Preview>
    <Story name="CircleLoader2" args={{ size: 'S' }}>
        {Template.bind({})}
    </Story>
</Preview>

### Color Variations

You can either change the theme or pass the colors as props.

<Preview withToolbar>
    <div style={{ display: 'flex', justifyContent: 'space-around' }}>
        <CircleLoader2 size="S" Color="green" />
        <CircleLoader2 size="S" Color="red" />
    </div>
</Preview>

### Sizes

There are Six sizs available `XXS` | `XS` | `S` | `M` | `L` | `XL`.

<Preview withToolbar>
    <div style={{ display: 'flex', justifyContent: 'space-between' }}>
        <DotsCircle size="XXS" />
        <CircleLoader2 size="XS" />
        <CircleLoader2 size="S" />
        <CircleLoader2 size="M" />
        <CircleLoader2 size="L" />
        <CircleLoader2 size="XL" />
    </div>
</Preview>

### Props

<Props story="CircleLoader2" />
