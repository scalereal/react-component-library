import { Button, TypeMap, SizeMap } from '.';
import { Preview, Story, Meta, Props, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';
import { withKnobs, color, boolean, select } from '@storybook/addon-knobs';

<Meta
    title="Components/Core/Button"
    component={Button}
    argTypes={{
        size: {
            control: {
                type: 'select',
                options: Object.values(SizeMap)
            }
        },
        textSize: {
            control: {
                type: 'select',
                options: Object.values(SizeMap)
            }
        },
        type: {
            control: {
                type: 'select',
                options: Object.values(TypeMap)
            }
        },
        bgColor: {
            control: 'color'
        },
        textColor: {
            control: 'color'
        },
        hoverBgColor: {
            control: 'color'
        },
        hoverTextColor: {
            control: 'color'
        },
        hoverShadowColor: {
            control: 'color'
        },
        hoverEffect: {
            control: { type: 'boolean' }
        },
        disabled: {
            control: { type: 'boolean' }
        },
        ref: {
            table: {
                disable: true
            }
        },
        icon: {
            table: {
                disable: true
            }
        },
        className: {
            table: {
                disable: true
            }
        }
    }}
/>

# Button

Displays a Button

export const Template = args => (
    <div>
        <Button {...args} />
    </div>
);

<Preview>
    <Story name="Button" args={{ size: 'S', children: 'Click Me' }}>
        {Template.bind({})}
    </Story>
</Preview>

### Color Variations

You can either change the theme or pass the colors as props.

<Preview withToolbar>
    <div>
        <Button size="S" hoverEffect={true} bgColor="green" textColor="white" hoverBgColor="blue">
            Click Me
        </Button>
        <Button size="S" hoverEffect={true} bgColor="red" textColor="white" hoverBgColor="green">
            Click Me
        </Button>
    </div>
</Preview>

### size

There are four sizes available `XS` | `S` | `M` | `L` | `XL`.

<Preview withToolbar>
    <div>
        <Button size="XS" hoverEffect={true}>
            Click Me
        </Button>
        <Button size="S" hoverEffect={true}>
            Click Me
        </Button>
        <Button size="M" hoverEffect={true}>
            Click Me
        </Button>
        <Button size="L" hoverEffect={true}>
            Click Me
        </Button>
        <Button size="XL" hoverEffect={true}>
            Click Me
        </Button>
    </div>
</Preview>

### textSize

There are four Text Sizes available `XS` | `S` | `M` | `L` | `XL`.

<Preview withToolbar>
    <div>
        <Button textSize="XS" hoverEffect={true}>
            Click Me
        </Button>
        <Button textSize="S" hoverEffect={true}>
            Click Me
        </Button>
        <Button textSize="M" hoverEffect={true}>
            Click Me
        </Button>
        <Button textSize="L" hoverEffect={true}>
            Click Me
        </Button>
        <Button textSize="XL" hoverEffect={true}>
            Click Me
        </Button>
    </div>
</Preview>

### Button Types

There are five types available `primary` | `secondary` | `warning` | `danger` | `success`.

<Preview withToolbar>
    <div>
        <Button size="S" hoverEffect={true} type="primary">
            Click Me
        </Button>
        <Button size="S" hoverEffect={true} type="secondary">
            Click Me
        </Button>
        <Button size="S" hoverEffect={true} type="warning">
            Click Me
        </Button>
        <Button size="S" hoverEffect={true} type="danger">
            Click Me
        </Button>
        <Button size="S" hoverEffect={true} type="success">
            Click Me
        </Button>
    </div>
</Preview>

### Disabled Button

Disabled style for all types.

<Preview withToolbar>
    <div>
        <Button size="S" hoverEffect={true} type="primary" disabled>
            Click Me
        </Button>
        <Button size="S" hoverEffect={true} type="secondary" disabled>
            Click Me
        </Button>
        <Button size="S" hoverEffect={true} type="warning" disabled>
            Click Me
        </Button>
        <Button size="S" hoverEffect={true} type="danger" disabled>
            Click Me
        </Button>
        <Button size="S" hoverEffect={true} type="success" disabled>
            Click Me
        </Button>
    </div>
</Preview>

### Props

<Props story="Button" />
