import { FileInput, SizeMap } from '.';
import { Preview, Story, Meta, Props, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';
import { withKnobs, color, boolean, select } from '@storybook/addon-knobs';

<Meta
    title="Components/Core/FileInput"
    component={FileInput}
    argTypes={{
        size: {
            control: {
                type: 'select',
                options: Object.values(SizeMap)
            }
        },
        hoverEffect: {
            control: { type: 'boolean' }
        },
        disabled: {
            control: { type: 'boolean' }
        },
        hasError: {
            control: { type: 'boolean' }
        },
        errorText: {
            control: { type: 'text' }
        },
        textColor: {
            control: 'color'
        },
        btnBgColor: {
            control: 'color'
        },
        btnTextColor: {
            control: 'color'
        },
        borderColor: {
            control: 'color'
        },
        btnHoverBorderColor: {
            control: 'color'
        },
        btnHoverBgColor: {
            control: 'color'
        },
        ref: {
            table: {
                disable: true
            }
        },
        theme: {
            table: {
                disable: true
            }
        },
        onChange: {
            table: {
                disable: true
            }
        }
    }}
/>

# FileInput

Displays a FileInput

export const Template = args => (
    <div>
        <FileInput {...args} />
    </div>
);

<Preview>
    <Story name="FileInput" args={{ size: 'S' }}>
        {Template.bind({})}
    </Story>
</Preview>

### Color Variations

You can either change the theme or pass the colors as props.

<Preview withToolbar>
    <div>
        <FileInput textColor="blue" btnBgColor="red" btnTextColor="white" borderColor="blue" />
        <FileInput textColor="red" btnBgColor="blue" btnTextColor="white" borderColor="red" />
    </div>
</Preview>

### Sizes

There are five sizs available `XS` | `S` | `M` | `L` | `XL`.

<Preview withToolbar>
    <div>
        <FileInput size="XS" />
        <FileInput size="S" />
        <FileInput size="M" />
        <FileInput size="L" />
        <FileInput size="XL" />
    </div>
</Preview>

### Error

Displays error if hasError={true}

<Preview withToolbar>
    <div>
        <FileInput hasError={true} errorText="Please choose Correct File" />
    </div>
</Preview>

### Disabled

Disabled FileInput

<Preview withToolbar>
    <div>
        <FileInput disabled />
    </div>
</Preview>

### Props

<Props story="FileInput" />
