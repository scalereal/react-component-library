import Avatar, { DisplayMap, SizeMap } from '.';
import { Preview, Story, Meta, Props, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';
import { withKnobs, color, boolean, select } from '@storybook/addon-knobs';

<Meta
    title="Components/Core/Avatar"
    component={Avatar}
    argTypes={{
        size: {
            control: {
                type: 'select',
                options: Object.values(SizeMap)
            },
            description: 'Avatar Sizes `XXS` | `XS` | `S` | `M` | `L` | `XL`'
        },
        res: {
            control: { type: 'text' },
            description: 'Avatar resource type `string` provide url or name'
        },
        bgColor: {
            control: 'color',
            description: '`string`'
        },
        textColor: {
            control: 'color',
            description: '`string`'
        },
        hoverBgColor: {
            control: 'color',
            description: '`string`'
        },
        hoverTextColor: {
            control: 'color',
            description: '`string`'
        },
        hoverShadowColor: {
            control: 'color',
            description: '`string`'
        },
        hoverTextShadowColor: {
            control: 'color',
            description: '`string`'
        },
        hoverEffect: {
            control: { type: 'boolean' },
            description: '`boolean`'
        },
        disabled: {
            control: { type: 'boolean' },
            description: '`boolean`'
        },
        display: {
            control: {
                type: 'select',
                options: Object.values(DisplayMap)
            },
            description: 'type `string` value `inline-block` | `block` '
        },
        ref: {
            table: {
                disable: true
            }
        },
        theme: {
            table: {
                disable: true
            }
        }
    }}
/>

# Avatar

Displays an image or text that represents a user or organization

export const Template = args => (
    <div>
        <Avatar
            display="inline-block"
            res="https://media.istockphoto.com/photos/child-hands-formig-heart-shape-picture-id951945718?k=6&m=951945718&s=612x612&w=0&h=ih-N7RytxrTfhDyvyTQCA5q5xKoJToKSYgdsJ_mHrv0="
            {...args}
        />
        <Avatar display="inline-block" res="pradip bhusnar" {...args} />
    </div>
);

<Preview>
    <Story name="Avatar" args={{ size: 'S' }}>
        {Template.bind({})}
    </Story>
</Preview>

### Color Variations

You can either change the theme or pass the colors as props.

<Preview withToolbar>
    <div>
        <Avatar
            display="inline-block"
            size="S"
            res="Pradip Bhusnar"
            hoverEffect={true}
            bgColor="green"
            textColor="white"
            hoverBgColor="blue"
        />
        <Avatar
            display="inline-block"
            size="S"
            res="Pradip Bhusnar"
            hoverEffect={true}
            bgColor="red"
            textColor="white"
            hoverBgColor="green"
        />
    </div>
</Preview>

### Sizes

There are Six sizs available `XXS` | `XS` | `S` | `M` | `L` | `XL`.

<Preview withToolbar>
    <div>
        <Avatar display="inline-block" size="XXS" res="Pradip Bhusnar" hoverEffect={true} />
        <Avatar display="inline-block" size="XS" res="Pradip Bhusnar" hoverEffect={true} />
        <Avatar display="inline-block" size="S" res="Pradip Bhusnar" hoverEffect={true} />
        <Avatar display="inline-block" size="M" res="Pradip Bhusnar" hoverEffect={true} />
        <Avatar display="inline-block" size="L" res="Pradip Bhusnar" hoverEffect={true} />
        <Avatar display="inline-block" size="XL" res="Pradip Bhusnar" hoverEffect={true} />
    </div>
</Preview>

### Props

<Props story="Avatar" />
