import Link, { linkSizes } from './Link';
import { Preview, Story, Meta, Props, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';
import { withKnobs, color, boolean, select } from '@storybook/addon-knobs';

<Meta
    title="Components/Core/Link"
    component={Link}
    argTypes={{
        linkColor: {
            control: 'color'
        },
        hoverColor: {
            control: 'color'
        },
        activeColor: {
            control: 'color'
        },
        linkSize: {
            control: {
                type: 'select',
                options: Object.values(linkSizes)
            }
        }
    }}
/>

# Link

Displays a Link

export const Template = ({ linkSize, linkColor, hoverColor, activeColor, ...args }) => {
    return (
        <Link href="#" linkSize={linkSize} linkColor={linkColor} hoverColor={hoverColor} activeColor={activeColor} {...args}>
            View Link
        </Link>
    );
};

<Preview>
    <Story
        name="Link"
        args={{
            linkSize: 'S'
        }}
    >
        {Template.bind({})}
    </Story>
</Preview>

### Color Variations

You can either change the theme or pass the colors as props.

<Preview withToolbar>
    <div style={{ display: 'flex', flexWrap: 'wrap', justifyContent: 'space-around' }}>
        <Link href="#" linkColor="blue" hoverColor="red">
            View Link
        </Link>
        <Link href="#" linkColor="red" hoverColor="blue">
            View Link
        </Link>
    </div>
</Preview>

### Sizes

There are five sizes available `XS` | `S` | `M` | `L` | `XL`.

<Preview withToolbar>
    <div style={{ display: 'flex', flexWrap: 'wrap', justifyContent: 'space-around' }}>
        <Link href="#" linkSize="XS" linkColor="blue">
            View Link
        </Link>
        <Link href="#" linkSize="S" linkColor="blue">
            View Link
        </Link>
        <Link href="#" linkSize="M" linkColor="blue">
            View Link
        </Link>
        <Link href="#" linkSize="L" linkColor="blue">
            View Link
        </Link>
        <Link href="#" linkSize="XL" linkColor="blue">
            View Link
        </Link>
    </div>
</Preview>

### Props

<Props story="Link" />
